rules_version = '2';

// Default-deny: no access until F-04 implements ownerUid-based rules.
// These rules are intentionally closed for local emulator development.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
